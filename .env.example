# Supabase project credentials (shared Gogi project). Copy to .env and fill in.
VITE_SUPABASE_URL=https://your-project-ref.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-or-publishable-key

# Table prefix that scopes this user's tables inside the shared Supabase project.
# Every table/RPC in the migration is created with this prefix.
VITE_TABLE_PREFIX=usr_nmexs7bytxq2
